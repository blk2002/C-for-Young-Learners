.container {
  min-height: 100vh;
  background: var(--bg);
}

.loading {
  text-align: center;
  padding: 100rpx 0;
  color: var(--t3);
}

.empty-state {
  text-align: center;
  padding: 120rpx 30rpx;
}

.empty-state .icon {
  font-size: 100rpx;
  margin-bottom: 20rpx;
}

.empty-state .text {
  font-size: 28rpx;
  color: var(--t3);
}

.practice-container {
  padding: 30rpx;
}

.progress-header {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx 30rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
}

.progress-text {
  font-size: 26rpx;
  color: var(--t2);
  margin-bottom: 12rpx;
}

.progress-bar {
  height: 12rpx;
  background: var(--line);
  border-radius: 6rpx;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--danger), var(--danger));
  border-radius: 6rpx;
  transition: width 0.3s;
}

.question-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 40rpx 30rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
}

.question-header {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.question-number {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--t1);
  margin-right: 16rpx;
}

.question-type-tag {
  display: inline-block;
  padding: 4rpx 16rpx;
  background: var(--tint);
  color: var(--primary);
  font-size: 22rpx;
  border-radius: 20rpx;
}

.question-type-tag.fill {
  background: var(--warning-tint);
  color: var(--warning);
}

.wrong-info {
  font-size: 24rpx;
  color: var(--danger);
  margin-bottom: 20rpx;
  padding: 8rpx 16rpx;
  background: var(--danger-tint);
  border-radius: 8rpx;
  display: inline-block;
}

.question-text {
  font-size: 30rpx;
  color: var(--t1);
  line-height: 1.6;
  margin-bottom: 30rpx;
}

.options {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.option-item {
  display: flex;
  align-items: center;
  padding: 24rpx 28rpx;
  background: var(--bg);
  border: 2rpx solid var(--line);
  border-radius: 12rpx;
  transition: all 0.2s;
}

.option-item:active {
  background: var(--bg);
}

.option-item.selected {
  background: var(--tint);
  border-color: var(--primary);
}

.option-item.disabled {
  opacity: 0.7;
  pointer-events: none;
}

.option-item.correct {
  background: var(--ok-tint);
  border-color: var(--ok);
}

.option-item.right-answer {
  background: var(--warning-tint);
  border-color: var(--warning);
}

.option-letter {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  background: #fff;
  border-radius: 50%;
  font-size: 26rpx;
  font-weight: 600;
  color: var(--t2);
  margin-right: 20rpx;
  flex-shrink: 0;
}

.option-item.selected .option-letter {
  background: var(--primary);
  color: #fff;
}

.option-item.correct .option-letter {
  background: var(--ok);
  color: #fff;
}

.option-item.right-answer .option-letter {
  background: var(--warning);
  color: #fff;
}

.option-text {
  flex: 1;
  font-size: 28rpx;
  color: var(--t1);
  line-height: 1.5;
}

.fill-input-wrap {
  margin-bottom: 20rpx;
}

.input {
  width: 100%;
  height: 80rpx;
  padding: 0 28rpx;
  background: var(--bg);
  border: 2rpx solid var(--line);
  border-radius: 12rpx;
  font-size: 28rpx;
  color: var(--t1);
  line-height: 80rpx;
  box-sizing: border-box;
}

.input.disabled {
  background: var(--bg);
  color: var(--t3);
}

.result-section {
  margin-top: 30rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--line);
}

.result-correct {
  font-size: 28rpx;
  color: var(--ok);
  font-weight: 600;
}

.result-wrong {
  font-size: 28rpx;
  color: var(--danger);
  font-weight: 600;
}

.answer-section {
  margin-top: 20rpx;
}

.answer-label {
  font-size: 26rpx;
  color: var(--t2);
  margin-bottom: 8rpx;
}

.answer-text {
  font-size: 28rpx;
  color: var(--t1);
  margin-bottom: 16rpx;
}

.explanation-label {
  font-size: 26rpx;
  color: var(--t2);
  margin-bottom: 8rpx;
}

.explanation-text {
  font-size: 28rpx;
  color: var(--t2);
  line-height: 1.6;
}

.master-btn {
  margin-top: 30rpx;
  padding: 20rpx;
  background: var(--ok-tint);
  color: var(--ok);
  text-align: center;
  border-radius: 12rpx;
  font-size: 26rpx;
  font-weight: 500;
}

.master-btn:active {
  opacity: 0.8;
}

.nav-buttons {
  display: flex;
  gap: 24rpx;
  margin-bottom: 30rpx;
}

.nav-btn {
  flex: 1;
  text-align: center;
  padding: 24rpx 0;
  background: #fff;
  border: 2rpx solid var(--line);
  border-radius: 12rpx;
  font-size: 28rpx;
  color: var(--t2);
  transition: all 0.2s;
}

.nav-btn:active {
  background: var(--bg);
}

.nav-btn.disabled {
  opacity: 0.5;
  pointer-events: none;
}

.submit-section {
  margin-bottom: 20rpx;
}

.submit-btn {
  background: linear-gradient(135deg, var(--danger), var(--danger));
  color: #fff;
  font-size: 32rpx;
  font-weight: 600;
  text-align: center;
  padding: 28rpx 0;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 92, 114, 0.3);
  transition: all 0.2s;
}

.submit-btn:active {
  transform: scale(0.98);
  opacity: 0.9;
}

.result-summary {
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  text-align: center;
  font-size: 28rpx;
  color: var(--t1);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
  margin-bottom: 20rpx;
}

.result-summary text {
  color: var(--danger);
  font-weight: 600;
}

.master-all-btn {
  background: #fff;
  color: var(--ok);
  font-size: 28rpx;
  font-weight: 500;
  text-align: center;
  padding: 24rpx 0;
  border-radius: 16rpx;
  border: 2rpx solid var(--ok);
}

.master-all-btn:active {
  background: var(--ok-tint);
}

.bottom-space {
  height: 40rpx;
}
